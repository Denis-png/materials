<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>Lesson</title>
    <link rel="stylesheet" href="/styles.css" />
  </head>
  <body>
    <%-include("./nav.ejs") %>
    <main>
      <h2><%= lesson.name %></h2>
      <% if (lesson.description) { %>
      <p><%= lesson.description %></p>
      <% } else {%>
      <p class="warning">No description</p>
      <% } %>
    </main>
  </body>
</html>
